import { FC } from "react";

export const App: FC = () => {
  return (
    <div className="App">
      <header className="App-header">
        <p>Electron, Typescript, Vite and React.</p>
      </header>
    </div>
  );
};
